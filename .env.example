VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Optional: EAN barcode scan-to-prefill. Leave both unset to disable the
# "Scanner un code-barre" button entirely (the rest of the app is unaffected).
VITE_PUZZLE_LOOKUP_URL=https://your-puzzle-lookup-domain
VITE_PUZZLE_LOOKUP_API_KEY=same-shared-key-as-the-puzzle-lookup-service
